<?xml version="1.0" encoding="UTF-8"?>
<engine>
	<template selector="#new-banner" append="true">
			{for banner in jsonArrayBanner}
				<![CDATA[
				<p><a
					{if banner.postObjectJson.link == ''}
						href="#"
					{else}
						target="_blank"
						href="${banner.postObjectJson.link}"
					{/if} 
				class="banner_link"
				original-title="{if banner.postObjectJson.bannerNovo == 'true'}Publicado{else}Atualizado{/if} por ${banner.postObjectJson.usuario} em ${banner.postObjectJson.dataDeAtualizacao}">
					<img src="/s/banner/${banner.postObjectJson.id}" />
				</a></p>
				]]>
			{/for}	
	</template>
	<script>
		$('#new-banner img:first').load(function() {
            $('#new-banner a.banner_link').tipsy({html:true, gravity:'s'});
		});
	</script>
</engine>