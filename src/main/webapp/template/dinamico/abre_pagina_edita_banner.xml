<?xml version="1.0" encoding="UTF-8"?>
<engine>
	<template selector="#content_principal">

	<fieldset class="orderingButtons">
	<legend>Ordenação</legend>
			<input id="ordering-initial-date" class="btNone" type="button" value="Data inicial de exibição"/>
			<input id="ordering-final-date" class="btNone" type="button" value="Data final de exibição"/>
			<input id="ordering-creation-date" class="btNone" type="button" value="Data de criação"/>
	</fieldset>


		<div class="listagem-banner">
			{for banner in jsonArrayBanner}
					<div class="dados-banner">
						<table id="${banner.postObjectJson.id}">
							<tr><td><img src="/s/banner/${banner.postObjectJson.id}" /></td></tr>
							<tr><td>
								Titulo: 
								<div id="titulo_${banner.postObjectJson.id}">${banner.postObjectJson.titulo}</div>
							</td></tr>
							<tr><td>
								Disponível à partir de: 
								<div id="dataInicio_${banner.postObjectJson.id}">${banner.postObjectJson.dataInicio}</div>
							</td></tr>	
							<tr><td>
								Disponível até: 
								<div id="dataFim_${banner.postObjectJson.id}">${banner.postObjectJson.dataFim}</div>
							</td></tr>	
							<tr><td>
								Ultima modificação: 
								<div id="dataDeAtualizacao_${banner.postObjectJson.id}">${banner.postObjectJson.dataDeAtualizacao}</div>
							</td></tr>
							<tr><td>
								Link: 
								<div id="link_${banner.postObjectJson.id}">${banner.postObjectJson.link}</div>
							</td></tr>
						</table>
					</div>
			{/for}	
		</div>

	</template>
	<script>
		<!-- $('.form-editar-banner').each(function() {
		    dextranet.banner.inicializaDatepicker(false, $(this).find('ul li .date-picker.inicio'), $(this).find('ul li .date-picker.fim'));
		});
		
		$(".form-editar-banner").submit(function(){
			var form = $(this);
			dextranet.banner.editarBanner($(form).attr("id"));
			return false;
		}); -->

		$('#ordering-initial-date').click(function() {
			dextranet.banner.ordenaBanners('dataInicio', $('.dados-banner'));
		});

		$('#ordering-final-date').click(function() {
			dextranet.banner.ordenaBanners('dataFim', $('.dados-banner'));
		});

		$('#ordering-creation-date').click(function() {
			dextranet.banner.ordenaBanners('dataDeAtualizacao', $('.dados-banner'));
		});
	</script>
</engine>
